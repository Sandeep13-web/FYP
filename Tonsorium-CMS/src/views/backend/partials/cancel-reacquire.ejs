<div class="modal fade" id="cancelReacquire" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog" role="document">
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title"><%= translateLanguage('Reacquire Confirmation',session.translationdata)  %> </h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
        <div class="modal-body text-center">
            <br>
            <p><%= translateLanguage('Are you sure you want to reacquire?',session.translationdata)  %></p>
        </div>
        <div class="modal-footer modal-btn">
            <button type="button" class="btn btn-primary" id="<%= id %>" data-ankenId="<%= anken_id %>" data-contractId="<%= contractId %>" data-dismiss="modal">
                <%= translateLanguage('Ok',session.translationdata,'わかった') %>
            </button>
            <button type="button" class="btn main-cancel-btn" data-dismiss="modal">
                <%= translateLanguage('Cancel',session.translationdata,'キャンセル') %>
            </button>
        </div>
    </div>
    </div>
</div>